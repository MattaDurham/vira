{
  "_readme": "Copy this file to data/config.json and fill in your values. Every key is optional — an absent or empty value leaves its feature dormant. data/ is git-ignored, so nothing here ever reaches the repo.",
  "owner_name": "",
  "_owner_name": "Your first name — used in the cockpit greeting and the daily-brief narrative.",
  "crm_root": "~/.vira/crm",
  "_crm_root": "Directory holding people.json, master.json, profiles/. You normally never touch this: the Setup window's contact importers create and fill it. Point it elsewhere only if you already keep CRM data in this shape. Until people.json exists there, Vira boots in fixture mode with one demo contact (Vira themself).",
  "graph_email": "",
  "_graph_email": "Default account for the Connect M365 button and the cockpit banner.",
  "msgraph_client_id": "",
  "msgraph_tenant": "",
  "_msgraph": "Your own tenant's app registration for Microsoft Graph mail/calendar (public client flows enabled, device-code login). See README > Mail.",
  "notify_handle": "",
  "_notify_handle": "iMessage handle Vira pings for high-value inbound email (your own self-thread number, e.g. +12125551234). Empty = notifications off.",
  "notify_enabled": true,
  "imessage_reply_enabled": true,
  "_imessage_reply_enabled": "Read replies in the iMessage self-thread as instructions (server/inbound.py). Vira acts on reversible in-app things immediately and confirms once before anything that leaves this machine. Dormant unless notify_handle is set.",
  "inbound_ignore_prefixes": [],
  "_inbound_ignore_prefixes": "Prefixes of machine-sent texts that also land in the self-thread and must never be read as you talking (notify.py's own sends are already covered by the \"Vira: \" prefix and the send ledger).",
  "family_calendars": [],
  "_family_calendars": "Calendar names (as they appear in Calendar.app) tagged as family in the daily brief, e.g. [\"Family\", \"Kids\"].",
  "brief_remote_events": [],
  "_brief_remote_events": "Event-title substrings (case-insensitive) treated as remote/virtual in the daily brief, e.g. [\"Weekly Standup\"]. A remote event is never flagged as an overlap against a same-time in-person event (two remote events still conflict). M365 events also auto-detect via the Graph isOnlineMeeting flag.",
  "ai_backend": "cli",
  "_ai_backend": "\"cli\" uses the local claude CLI (Max plan); \"api\" uses the Anthropic API via the VIRA_ANTHROPIC_KEY env var.",
  "cli_model": "sonnet",
  "model_alias_overrides": {},
  "_model_alias_overrides": "Corrections for model aliases this machine's CLI resolves WRONG, e.g. {\"opus\": \"claude-opus-5\"} when --model opus still gives an older generation. Empty by default. Re-measure after a CLI upgrade with: claude --print --output-format json --model opus 'Say OK.' (read modelUsage) and DELETE the entry once the alias is correct again.",
  "_cli_model": "An ALIAS the CLI resolves to the newest model of that tier: sonnet | opus | haiku | fable. Never a full model id — an id here names a generation and starts lying the week the next one ships.",
  "api_model": "",
  "_api_model": "Empty = let Vira pick, at call time, the newest model of the cli_model tier from the list your API key can actually reach. Set a full id only to pin one deliberately.",
  "api_key_env": "VIRA_ANTHROPIC_KEY",
  "timeout": 120,
  "ai_health_enabled": true,
  "_ai_health_enabled": "Deterministic AI-backend health watcher: probes `claude auth status` (or the API key) on a cadence and texts you the moment the model login goes down, so an expired Claude session surfaces out-of-band instead of as a silently dead cockpit job. Uses none of the model itself. false = disable the watcher (the /api/health/ai probe still works on demand).",
  "idea_tag_interval_min": 10,
  "doc_tag_interval_min": 10,
  "_doc_tag_interval_min": "Minutes between Reader document tagging passes (server/doctags.py). Each tick tags at most ONE batch of 12, so a fresh library groups itself over an hour or two and nothing waits on it.",
  "doc_thumb_interval_min": 15,
  "_doc_thumb_interval_min": "Minutes between Reader document thumbnail sweeps (server/docthumbs.py). Dormant without a Chrome/Chromium/Edge on the machine; thumb_browser overrides the probe with an explicit binary path.",
  "thumb_browser": "",
  "_thumb_browser": "Explicit browser binary for document thumbnails; empty = probe PATH and app bundles.",
  "_idea_tag_interval_min": "Minutes between backlog tagging passes (server/ideatags.py). Each tick embeds any new ideas locally and makes at most ONE model call, so a fresh backlog tags itself over an hour rather than in one spend.",
  "ai_health_interval_min": 5,
  "_ai_health_interval_min": "Minutes between health probes (clamped to a 60s floor). The probe is cheap and spends no tokens.",
  "ai_health_notify": true,
  "_ai_health_notify": "Text the owner (via notify_handle) on a green->red edge and on recovery. Dormant unless notify_handle is set, like every other Vira notification.",
  "session_default_mode": "interactive",
  "_session_default_mode": "Live coding sessions, safest first: \"interactive\" pauses every risky tool call for your Approve/Deny in the job panel; \"acceptedits\" lets file edits land unasked but still asks before commands; \"autopilot\" bypasses all permission checks. Per-run override in the UI.",
  "session_auto_allow": [
    "Read",
    "Grep",
    "Glob",
    "TodoWrite",
    "Task",
    "NotebookRead",
    "WebSearch"
  ],
  "_session_auto_allow": "Read-only tools the permission gate approves without asking. Anything else that reaches the gate raises an Approve/Deny card in the session panel.",
  "session_permission_timeout": 600,
  "_session_permission_timeout": "Seconds an unanswered permission card waits before the gate DENIES it by default (the session then continues rather than hanging forever).",
  "session_max_live": 4,
  "_session_max_live": "Cap on concurrent live agent sessions (each holds a claude CLI subprocess against the Max plan). Launches past the cap are rejected with a clear error. Sessions parked in their reply window do not count -- they have finished their work and are only waiting on you.",
  "session_reply_window_hours": 12,
  "_session_reply_window_hours": "How long a session that has finished its turn stays open for your reply (status \"waiting on your reply\", compose bar live) before it finalizes on its own. Ending it deliberately with Finish is the normal path; this is only the safety reap for one you walked away from.",
  "session_landing_card": true,
  "_session_landing_card": "When a coding session you dispatched parks with work on its branch, Vira itself raises the merge / keep playing / discard decision card (in the terminal, the Attention window, and on your phone) instead of leaving that question in the transcript. false leaves the session to end however it ends.",
  "session_auto_serve": true,
  "_session_auto_serve": "Serve a passive, local-only test instance of the branch (scripts/branch.sh serve <slug> --local) before that card goes up, so the card carries a URL to look at. It is never bridged to the tailnet automatically -- the snapshot behind it is your own data.",
  "mercury_poll_hours": 6,
  "_mercury_poll_hours": "How often the subscriptions module polls Mercury for new charges. Dormant until a READ ONLY Mercury API token is in the Keychain: security add-generic-password -U -a mercury -s vira-mercury -w",
  "receipts_sweep_days": 7,
  "_receipts_sweep_days": "How often the receipts pass sweeps mailboxes + indexed attachments for invoice/receipt evidence (targeted-first: flagged merchants sweep before the rest). Dormant until a mail account is configured. On-demand per merchant via the card's Find receipts button.",
  "subs_notify_threshold_usd": 100,
  "_subs_notify_threshold_usd": "Renewal-ping floor: iMessage when a renewal is within 7 days and the per-cycle amount clears this (annual renewals always ping). Uses notify_handle; deduped per merchant + renewal date.",
  "vault_root": "",
  "_vault_root": "Notes vault the Brain window indexes (Obsidian or any folder of markdown; FTS + local nomic embeddings via Ollama; nothing leaves the machine at index time). Set it from Setup > Brain — point at an existing vault or start a fresh one there. Empty = the Brain stays dormant.",
  "reader_sources": [],
  "_reader_sources": "Folders outside Vira connected to the Reader, as a list of {path, kind, glob, label}. Each is READ and never written or copied: a matching file is one document, and a subdirectory holding index.html is one document too (a page plus its assets). kind is dossier | plan | retro | brief | walkthrough (default dossier); glob is a filename pattern inside the folder (default *.html); label names the source. Documents appear after the Reader's Scan. Removing a folder here immediately stops Vira serving anything out of it. Example: [{\"path\": \"~/workspace/crm/data/self/renderings/current\", \"kind\": \"dossier\", \"label\": \"Self-record renderings\"}]",
  "vault_dirs": [],
  "_vault_dirs": "Vault subdirectories to index (recursive), plus root-level .md files always. Empty = the default set (wiki, Briefs, Sessions, retros, brain-retros).",
  "vault_sources": [],
  "_vault_sources": "Additional read-only markdown vaults searched by Find and Chat with my Vault. Each entry is {id, name, root, dirs?}. The primary vault_root remains the write target for plans, definitions, and ingestion.",
  "site_root": "",
  "_site_root": "Checkout of the owner's public site repo (e.g. ~/workspace/thedurham-nyc). Used by the sitedocs migration (documents moved off the site into /docs/) and the blog publish pipeline (posts render into the site repo and push). Empty = both stay dormant.",
  "judge_model": "opus",
  "chat_model": "",
  "_judge_model": "Model for fresh-eyes judge sessions (circuit judge stages and the Judge button on job history). Accepts sonnet | opus | haiku | fable.",
  "atlas_anchor_org": "",
  "_atlas_anchor_org": "Company name pinned as the Contact Atlas anchor cluster (substring-matched against CRM company fields), e.g. \"Taurid Capital\". Empty = no pinned cluster; communities still form by label propagation.",
  "atlas_max_nodes": 200,
  "_atlas_max_nodes": "Contact Atlas node cap - the N most-active tiered contacts rendered in the graph. Keeps the mesh legible and the O(n^2) edge passes cheap.",
  "atlas_min_edge_weight": 0.15,
  "_atlas_min_edge_weight": "Edges whose fused multi-signal weight falls below this are dropped from the atlas.",
  "circle_refresh_min": 60,
  "_circle_refresh_min": "Minutes between circle sync passes (server/circles.py): re-matches the Visual Network's social circles to their stable identities and re-reads (one model call each, at most four per pass) only the circles whose members or shared group chats changed.",

  "media_archive_enabled": true,
  "_media_archive_enabled": "Keep Vira's own copy of every iMessage attachment. macOS treats ~/Library/Messages/Attachments as an evictable cache - Messages in iCloud offloads the bytes under storage pressure and leaves the chat.db row - so without this a conversation's media history decays into a list of filenames.",
  "media_archive_root": "",
  "_media_archive_root": "Where the archive lives. Empty = data/media-archive. Point it at an external volume to keep it off the boot disk, whose fullness is what triggers the eviction in the first place.",
  "media_archive_max_gb": 0,
  "_media_archive_max_gb": "Size cap in GB; 0 = no cap. A reached cap is reported in the sweep log and in the status payload, never silently applied.",
  "media_archive_interval_min": 30,
  "_media_archive_interval_min": "Background sweep cadence. Each pass only has to copy what macOS has downloaded since the last one.",
  "companion_hub_url": "",
  "_companion_hub_url": "URL the Android companion pairing QR points the phone at, e.g. http://your-machine.your-tailnet.ts.net:8377. Empty = auto-detect: the Tailscale MagicDNS name, then the tailnet IP, then the LAN IP.",
  "launchd_label": "",
  "_launchd_label": "macOS: if Vira runs as a launchd service, its label (e.g. com.example.vira). The in-app updater then restarts via launchctl kickstart; empty = the updater refuses web-triggered restarts (nothing would relaunch the server).",
  "windows_task_name": "",
  "_windows_task_name": "Windows analog of launchd_label: the Task Scheduler task that keeps Vira running (scripts\\run.ps1 -Register creates it, default name \"Vira\", and records it here). The updater restarts by exiting and letting the task's relaunch loop bring the new code up; empty = the updater refuses web-triggered restarts on Windows.",
  "apple_contacts_push": true,
  "_apple_contacts_push": "macOS: contact-card saves also update the matching Apple Contacts card (company, birthday, email/phone order + labels), which syncs to the phone. false disables.",
  "fixture_mode": null,
  "_fixture_mode": "true/false forces demo/real mode; null (default) auto-detects: fixture when crm_root is missing.",
  "lab_root": "",
  "_lab_root": "Local checkout dir holding careers-teardown explorer directories (each with a data.js) to ingest as STATIC role corpora. Optional: the Applications module's normal source is the live board poller, which its front door sets up for you. applications_sources (list of {slug, company, path}) overrides it.",
  "self_record": "",
  "_self_record": "The folder holding canon/MASTER_HISTORY.md (the career record whose endnotes are the claim gate every application is checked against), your resume, and every generated package. Written by the Applications front door; set it by hand only if you already keep a record somewhere. Empty = <crm_root>/self.",
  "applications_universe": "",
  "_applications_universe": "Where the scored candidate universe lives (candidate-universe/role/*.json plus *-raw-scores.json). Written by the front door. Empty = <self_record>/analysis.",
  "applications_packages_root": "",
  "_applications_packages_root": "Folder holding one generated package per application (resume, cover letter, and interview materials). Empty = ~/Documents/CV/15-applications when that folder exists, otherwise <self_record>/15-applications.",
  "applications_locations": [],
  "_applications_locations": "Office place names you will work in, e.g. [\"Berlin\", \"BER\"]. EMPTY MEANS UNFILTERED unless another location setting is configured. Eligibility gates the phone ping and the default view, never what gets fetched.",
  "applications_remote_regions": [],
  "_applications_remote_regions": "Territories from which you can accept region-limited remote work, e.g. [\"Germany\", \"Central European Time\"]. This is private preference data. Empty means Vira does not infer territories from your office locations.",
  "applications_remote_exclude": [],
  "_applications_remote_exclude": "Optional private regex or list of remote-location labels that are outside your search. Empty means no geography is excluded by product default.",
  "applications_region_hints": [],
  "_applications_region_hints": "Optional private regex or list of labels that establish an acceptable region when a posting mixes Remote with several locations. Empty means no geography is accepted by product default.",
  "applications_remote_ok": true,
  "_applications_remote_ok": "Count remote postings as eligible. applications_remote_exclude / applications_region_hints (regex or list of substrings) can privately tune which remote postings are reachable; Vira ships with neither geography configured.",
  "boards_auto_score": true,
  "_boards_auto_score": "Score new eligible board roles automatically: after each sweep (and every few minutes between) the poller dispatches ONE scoring session at a time over the unscored backlog, batch-capped at 40 roles per session, gated on an AI actually being connected. false restores the manual Score-new button in the Applications boards strip.",
  "boards_score_model": "",
  "_boards_score_model": "Model for auto-dispatched scoring sessions. Empty = the session default (cli_model).",
  "boards_auto_rescore": true,
  "_boards_auto_rescore": "Once nothing is unscored, drain the STALE backlog the same way: roles whose analysis was written before your record last changed get re-judged, one session at a time behind the same guards, ordered tier-first then oldest so the roles you would act on come back first. This is real spend on a large backlog - false stops the drain and leaves the per-role Rescore button as the only path.",
  "pivot_keywords": [],
  "_pivot_keywords": "Extra words widening the Radar 'Reconnect — pivot' fingerprint (server/reconnect.py). The target companies and lanes are always derived live from the Applications universe; this only adds keywords the deterministic profile-overlap scan should also treat as pivot-relevant.",
  "whatsapp_bridge_port": 18377,
  "_whatsapp_bridge_port": "Linked-device WhatsApp sidecar port (127.0.0.1 only). Dormant until paired via settings > WhatsApp. whatsapp_poll_seconds (default 5) and whatsapp_node_bin (default \"node\") tune the watcher and node binary.",
  "sms_fallback": true,
  "_sms_fallback": "When an iMessage is not delivered (an Android/iMessage-less recipient), re-send it as a plain text. Needs Text Message Forwarding to the paired iPhone so Messages owns an SMS account. send_verify_seconds (default 8) is how long Vira watches chat.db for the delivery error before giving up (0 disables the reactive check). A contact can also be marked SMS on their person page, which sends as a text from the start.",
  "evidence_retro_dir": "",
  "_evidence_retro_dir": "Directory the Evidence Ledger mines session retros from (glob \"* vira.md\"). Empty = ~/TC-IL/Sessions. The ledger also reads this checkout's own git-ignored retros/*.md when present.",
  "lessons_ledger_path": "",
  "_lessons_ledger_path": "The corrections ledger the lesson-recurrence counter reads (server/lessonwatch.py, Work > RECORD > Rules). Empty = ~/.claude/LESSONS.md; a missing file leaves the module dormant. Vira only ever READS this file.",
  "lessons_state_dir": "",
  "_lessons_state_dir": "The provenance stores beside the ledger: results/<day>/*.json session digests plus lessons-proposed.jsonl / lessons-decided.jsonl. Empty = ~/.claude/sessions.",
  "lessons_retro_dirs": "",
  "_lessons_retro_dirs": "Comma-separated retro directories the counter reads as the markdown fallback (ALL projects, not just Vira). Empty = evidence_retro_dir (or its default) plus this checkout's retros/.",
  "lesson_promote_at": 3,
  "_lesson_promote_at": "Distinct sessions that must describe breaking a tier-2 rule before the counter stages a promotion proposal (a `proposed` idea in the Queue — never a ledger write).",
  "lesson_candidates_per_rule": 12,
  "_lesson_candidates_per_rule": "Rung-2 cost ceiling: at most this many candidate reversals are adjudicated per rule per pass (newest first; drops are counted in the run record, never silent).",
  "ytdlp_path": "",
  "_ytdlp_path": "yt-dlp binary used by the reading-room full ingest to stage YouTube caption transcripts into the vault (server/fullingest.py). Empty = probe PATH; absent binary = YouTube items report 'yt-dlp not installed' and articles still stage."
}
